<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ecomZadanie.Views.UserDetailsPage">
    <StackLayout>
        <Label Text="{Binding UserData.Id}"/>
        <Label Text="{Binding UserData.FirstName}"/>
        <Label Text="{Binding UserData.LastName}"/>
        <Label Text="{Binding UserData.Age}"/>
        <Label Text="{Binding UserData.City}"/>
    </StackLayout>
</ContentPage>